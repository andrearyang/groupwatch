import React from 'react';
import { Inner } from './styles/jumbotron';

export default function Jumbotron({}) {
    return(
        <Inner>
            <p>I am so the Jumbotron!!</p>
        </Inner>
    )
}